/*
(function ($) {
  'use strict';

  $('html').attr('ng-app', 'revaluate');

  // ---
  // Auto-Bootstrap our angular app.
  // ---
  angular
    .module('config')
    .constant('APP_CONFIG', window.APP_CONFIG_SKELETON)
    .constant('APP_STATS', window.APP_STATS_SKELETON);
}

(window.jQuery));
*/
